# Optional build-time defaults for the Supabase connection.
# These pre-fill Settings; users can also enter/override them at runtime.
# The publishable (anon) key is client-safe. NEVER put a service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_WORKSPACE_ID=
